// funciones que retornan valores

const sumar = function sumar (num1, num2){
    return num1 + num2;
}

console.log(sumar(30,30));

//arrow function
const restar = (a, b) => a - b; //cuando esta en una sola linea no necesita poner return

console.log(restar(10, 5));